@extends('layouts.app')

@section('content')

<div class="container">

  Tournament

<!--     @yield('controls')

    <chat-component
      :player="{{ $player }}"
      :user="{{ Auth::user() }}"
      :tournament="{{ $tournament }}">
    </chat-component>

    @include('partials.bank')

    @include('partials.teams') -->

</div>
@endsection